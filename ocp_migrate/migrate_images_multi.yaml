clusters:
  from:
    name: <str | nombre del cluster origen>
    namespaces: <List[str] | namespaces a migrar>
    url_public_registry: <str | url de la registry publica del cluster para armar la url final y hacer el pull>
  to:
    name: <str | nombre del cluster destino>
jaime:
  url: <str | url de jaime>


#####################################

# Migra las imagenes dentro de los imagestream del cluster usando el comando "oc import-image ..." 
# de los namespaces enviados por parametro